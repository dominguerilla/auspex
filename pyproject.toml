[project]
name = "auspex"
version = "0.1.0"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line too long — CONCEPT docstrings are intentionally verbose
]

[tool.pytest.ini_options]
testpaths = ["tests"]
